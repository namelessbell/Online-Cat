<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CAT</title>

    <link rel="stylesheet" href="https://unpkg.com/swiper@7/swiper-bundle.min.css" />

    <!-- font awesome cdn link  -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

    <!-- custom css file link  -->
    <link href= "/public/css/style.css" rel="stylesheet" type="text/css" />

</head>
<body>
    
  <%- include('userheader'); -%>

  <section class="arrivals" id="arrivals">

    <% if(kucing.length>0) { %>
        <div class="swiper arrivals-slider">
            <% for(var i=0; i<kucing.length;i++) { %>
                <a href="/catdetails/<%=kucing[i]._id%>" class="swiper-slides box">
                    <div class="image">
                        <img src="/uploads/<%=kucing[i].image%>" alt="" style="width:250px;height:320px;">
                    </div>
                    <div class="content">
                        <h3><%= kucing[i].name%></h3>
                        <div class="price">Name/ Type: <%= kucing[i].name%></div><br>
                        <div class="price">Color: <%= kucing[i].color%></div>
                        <div class="price">Size: <%= kucing[i].size%></div>
                        <div class="price">State: <%= kucing[i].state%></div>
                    </div>
                </a>
                <% } %> 
        </div>
    
    
    <% } else { %>
        <h1 class="heading"> <span>No product found</span> </h1>
    <% } %>

</section>